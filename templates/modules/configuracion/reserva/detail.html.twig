<div class="row">
    <div class="col-6">
        <h5 class="mb-0"><b>Nombre:</b> {{ item.nombre }}</h5>
        <h5 class="mb-0"><b>Enlace:</b> {{ item.enlace | default('') }}</h5>
    </div>
    <div class="col-6">
        {% if '1' == item.activo %}
            <h5 class="mb-0"><b>Estado:</b> Sí</h5>
        {% else %}
            <h5 class="mb-0"><b>Estado:</b> No</h5>
        {% endif %}
    </div>
</div>

