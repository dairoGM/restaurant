{% extends 'baseAdmin.html.twig' %}

{% block title %}Configurar galería{% endblock %}

{% block body %}
    <div class="card shadow-sm">
        <div class="card-header">
            <div class="row" style="display: flex; justify-content: space-between; align-items: center">
                <h3 class="card-title text-bold text-gray">Configurar galería</h3>
            </div>
        </div>
        <!-- /.card-header -->


        <div class="card-body">
            <div class="row">
                <div class="col-4">
                    <h5 class="mb-0"><b>Nombre corto:</b> {{ espacio.nombreCorto | default('') }}</h5>
                </div>
                <div class="col-4">
                    {% if '1' == espacio.activo %}
                        <h5 class="mb-0"><b>Estado:</b> Habilitado</h5>
                    {% else %}
                        <h5 class="mb-0"><b>Estado:</b> Deshabilitado</h5>
                    {% endif %}
                </div>
                <div class="col-4">
                    {% if '1' == espacio.publico %}
                        <h5 class="mb-0"><b>Publico:</b> Sí</h5>
                    {% else %}
                        <h5 class="mb-0"><b>Publico:</b> No </h5>
                    {% endif %}
                </div>
            </div>
            <hr>
            {{ form_start(form) }}

            <div class="row">
                <div class="col-4 form-group">
                    <div class="row">
                        <div class="col-10">
                            {{ form_label(form.imagen1) }}
                            {{ form_widget(form.imagen1) }}
                            {{ form_errors(form.imagen1) }}
                        </div>
                        <div class="col-2">
                            <div id="image-holder1"></div>

                            {% if espacio.imagen1 != null %}
                                <img src="{{ app.request.getSchemeAndHttpHost() }}/uploads/images/espacio/imagen1/{{ espacio.imagen1 }}"
                                     class="img-circle imgp" alt="Imagen banner"
                                     style="width: 40px; height: 40px; position: absolute; bottom: 0;">
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="col-4 form-group">
                    <div class="row">
                        <div class="col-10">
                            {{ form_label(form.imagen2) }}
                            {{ form_widget(form.imagen2) }}
                            {{ form_errors(form.imagen2) }}
                        </div>
                        <div class="col-2">
                            <div id="image-holder2"></div>

                            {% if espacio.imagen2 != null %}
                                <img src="{{ app.request.getSchemeAndHttpHost() }}/uploads/images/espacio/imagen2/{{ espacio.imagen2 }}"
                                     class="img-circle imgp" alt="Imagen banner"
                                     style="width: 40px; height: 40px; position: absolute; bottom: 0;">
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="col-4 form-group">
                    <div class="row">
                        <div class="col-10">
                            {{ form_label(form.imagen3) }}
                            {{ form_widget(form.imagen3) }}
                            {{ form_errors(form.imagen3) }}
                        </div>
                        <div class="col-2">
                            <div id="image-holder3"></div>

                            {% if espacio.imagen3 != null %}
                                <img src="{{ app.request.getSchemeAndHttpHost() }}/uploads/images/espacio/imagen3/{{ espacio.imagen3 }}"
                                     class="img-circle imgp" alt="Imagen banner"
                                     style="width: 40px; height: 40px; position: absolute; bottom: 0;">
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="col-4 form-group">
                    <div class="row">
                        <div class="col-10">
                            {{ form_label(form.imagen4) }}
                            {{ form_widget(form.imagen4) }}
                            {{ form_errors(form.imagen4) }}
                        </div>
                        <div class="col-2">
                            <div id="image-holder4"></div>

                            {% if espacio.imagen4 != null %}
                                <img src="{{ app.request.getSchemeAndHttpHost() }}/uploads/images/espacio/imagen4/{{ espacio.imagen4 }}"
                                     class="img-circle imgp" alt="Imagen banner"
                                     style="width: 40px; height: 40px; position: absolute; bottom: 0;">
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="col-4 form-group">
                    <div class="row">
                        <div class="col-10">
                            {{ form_label(form.imagen5) }}
                            {{ form_widget(form.imagen5) }}
                            {{ form_errors(form.imagen5) }}
                        </div>
                        <div class="col-2">
                            <div id="image-holder5"></div>

                            {% if espacio.imagen5 != null %}
                                <img src="{{ app.request.getSchemeAndHttpHost() }}/uploads/images/espacio/imagen5/{{ espacio.imagen5 }}"
                                     class="img-circle imgp" alt="Imagen banner"
                                     style="width: 40px; height: 40px; position: absolute; bottom: 0;">
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="col-4 form-group">
                    <div class="row">
                        <div class="col-10">
                            {{ form_label(form.imagen6) }}
                            {{ form_widget(form.imagen6) }}
                            {{ form_errors(form.imagen6) }}
                        </div>
                        <div class="col-2">
                            <div id="image-holder6"></div>

                            {% if espacio.imagen6 != null %}
                                <img src="{{ app.request.getSchemeAndHttpHost() }}/uploads/images/espacio/imagen6/{{ espacio.imagen6 }}"
                                     class="img-circle imgp" alt="Imagen banner"
                                     style="width: 40px; height: 40px; position: absolute; bottom: 0;">
                            {% endif %}
                        </div>
                    </div>
                </div>
                {#                <div class="col-4 form-group" style="margin-left: 94%"> #}
                {#                    <br> #}
                {#                    <button id="btn_guardar" class="btn btn-primary">Aceptar</button> #}
                {#                </div> #}
            </div>
            <div class="card-footer" style="display: flex; justify-content: flex-end; align-items: center;">
                <a class="btn btn-danger mr-2 btn-cancel" data-url="{{ path('app_espacio_index') }}" data-title=""
                   data-content="¿Está seguro que desea realizar la acción?" type="button">Cancelar</a>
                <button id="btn_guardar" class="btn btn-primary">Aceptar</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        $('#btn_guardar').click(function (e) {
            HoldOn.open({
                theme: "sk-cube-grid",
                message: 'Por favor espere...',
                textColor: "white"
            });
            e.preventDefault();
            $('form[name*=espacio_galeria]').prop('action', 'galeria');
            $('form[name*=espacio_galeria]').submit();
        });

        $("#espacio_galeria_imagen1").on('change', function () {
            //Get count of selected files
            var countFiles = $(this)[0].files.length;
            var imgPath = $(this)[0].value;

            var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
            var image_holder = $("#image-holder1");
            image_holder.empty();
            if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
                if (typeof (FileReader) != "undefined") {
                    //loop for each file selected for uploaded.
                    for (var i = 0; i < countFiles; i++) {
                        var reader = new FileReader();
                        reader.onload = function (e) {

                            $('.imgp').remove()
                            $("<img />", {
                                "src": e.target.result,
                                "class": "thumb-image   img-circle",
                                "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
                            }).appendTo(image_holder);
                        }
                        image_holder.show();
                        reader.readAsDataURL($(this)[0].files[i]);
                    }
                } else {
                    alert("This browser does not support FileReader.");
                }
            } else {
                alert("Pls select only images");
            }
        });

        $("#espacio_galeria_imagen2").on('change', function () {
            //Get count of selected files
            var countFiles = $(this)[0].files.length;
            var imgPath = $(this)[0].value;

            var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
            var image_holder = $("#image-holder2");
            image_holder.empty();
            if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
                if (typeof (FileReader) != "undefined") {
                    //loop for each file selected for uploaded.
                    for (var i = 0; i < countFiles; i++) {
                        var reader = new FileReader();
                        reader.onload = function (e) {

                            $('.imgp').remove()
                            $("<img />", {
                                "src": e.target.result,
                                "class": "thumb-image   img-circle",
                                "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
                            }).appendTo(image_holder);
                        }
                        image_holder.show();
                        reader.readAsDataURL($(this)[0].files[i]);
                    }
                } else {
                    alert("This browser does not support FileReader.");
                }
            } else {
                alert("Pls select only images");
            }
        });

        $("#espacio_galeria_imagen3").on('change', function () {
            //Get count of selected files
            var countFiles = $(this)[0].files.length;
            var imgPath = $(this)[0].value;

            var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
            var image_holder = $("#image-holder3");
            image_holder.empty();
            if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
                if (typeof (FileReader) != "undefined") {
                    //loop for each file selected for uploaded.
                    for (var i = 0; i < countFiles; i++) {
                        var reader = new FileReader();
                        reader.onload = function (e) {

                            $('.imgp').remove()
                            $("<img />", {
                                "src": e.target.result,
                                "class": "thumb-image   img-circle",
                                "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
                            }).appendTo(image_holder);
                        }
                        image_holder.show();
                        reader.readAsDataURL($(this)[0].files[i]);
                    }
                } else {
                    alert("This browser does not support FileReader.");
                }
            } else {
                alert("Pls select only images");
            }
        });

        $("#espacio_galeria_imagen4").on('change', function () {
            //Get count of selected files
            var countFiles = $(this)[0].files.length;
            var imgPath = $(this)[0].value;

            var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
            var image_holder = $("#image-holder4");
            image_holder.empty();
            if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
                if (typeof (FileReader) != "undefined") {
                    //loop for each file selected for uploaded.
                    for (var i = 0; i < countFiles; i++) {
                        var reader = new FileReader();
                        reader.onload = function (e) {

                            $('.imgp').remove()
                            $("<img />", {
                                "src": e.target.result,
                                "class": "thumb-image   img-circle",
                                "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
                            }).appendTo(image_holder);
                        }
                        image_holder.show();
                        reader.readAsDataURL($(this)[0].files[i]);
                    }
                } else {
                    alert("This browser does not support FileReader.");
                }
            } else {
                alert("Pls select only images");
            }
        });

        $("#espacio_galeria_imagen5").on('change', function () {
            //Get count of selected files
            var countFiles = $(this)[0].files.length;
            var imgPath = $(this)[0].value;

            var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
            var image_holder = $("#image-holder5");
            image_holder.empty();
            if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
                if (typeof (FileReader) != "undefined") {
                    //loop for each file selected for uploaded.
                    for (var i = 0; i < countFiles; i++) {
                        var reader = new FileReader();
                        reader.onload = function (e) {

                            $('.imgp').remove()
                            $("<img />", {
                                "src": e.target.result,
                                "class": "thumb-image   img-circle",
                                "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
                            }).appendTo(image_holder);
                        }
                        image_holder.show();
                        reader.readAsDataURL($(this)[0].files[i]);
                    }
                } else {
                    alert("This browser does not support FileReader.");
                }
            } else {
                alert("Pls select only images");
            }
        });

        $("#espacio_galeria_imagen6").on('change', function () {
            //Get count of selected files
            var countFiles = $(this)[0].files.length;
            var imgPath = $(this)[0].value;

            var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
            var image_holder = $("#image-holder6");
            image_holder.empty();
            if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
                if (typeof (FileReader) != "undefined") {
                    //loop for each file selected for uploaded.
                    for (var i = 0; i < countFiles; i++) {
                        var reader = new FileReader();
                        reader.onload = function (e) {

                            $('.imgp').remove()
                            $("<img />", {
                                "src": e.target.result,
                                "class": "thumb-image   img-circle",
                                "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
                            }).appendTo(image_holder);
                        }
                        image_holder.show();
                        reader.readAsDataURL($(this)[0].files[i]);
                    }
                } else {
                    alert("This browser does not support FileReader.");
                }
            } else {
                alert("Pls select only images");
            }
        });

        // $("#espacio_galeria_galeria").on('change', function () {
        //     //Get count of selected files
        //     var countFiles = $(this)[0].files.length;
        //     var imgPath = $(this)[0].value;
        //
        //     var extn = imgPath.substring(imgPath.lastIndexOf('.') + 1).toLowerCase();
        //     var image_holder = $("#image-holder");
        //     image_holder.empty();
        //     if (extn == "gif" || extn == "png" || extn == "jpg" || extn == "jpeg") {
        //         if (typeof (FileReader) != "undefined") {
        //             //loop for each file selected for uploaded.
        //             for (var i = 0; i < countFiles; i++) {
        //                 var reader = new FileReader();
        //                 reader.onload = function (e) {
        //                     $("<img />", {
        //                         "src": e.target.result,
        //                         "class": "thumb-image   img-circle",
        //                         "style": 'width: 40px; height: 40px; position: absolute; bottom: 0;'
        //                     }).appendTo(image_holder);
        //                 }
        //                 image_holder.show();
        //                 reader.readAsDataURL($(this)[0].files[i]);
        //             }
        //         } else {
        //             alert("This browser does not support FileReader.");
        //         }
        //     } else {
        //         alert("Pls select only images");
        //     }
        // });
    </script>
{% endblock %}